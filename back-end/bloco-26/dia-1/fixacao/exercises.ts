const greeter = (name:string) => {
  return `Olá ${name}`;
};

const personAge = (name:string, age:number) => {
  return `${name} tem ${age} anos`;
};

const calcularLosango = (largerLozenge: number, lesserLozenge:number) => {
  return (largerLozenge * lesserLozenge) / 2;
};

